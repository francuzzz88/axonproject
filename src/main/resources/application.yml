spring:
  application:
    name: user-service
  datasource:
    url: jdbc:h2:file:~/data_h2/userDB
    username: root
    password:
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
      path: /h2

server:
  port: 8080


